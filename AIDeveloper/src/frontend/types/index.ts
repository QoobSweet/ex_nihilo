/**
 * Centralized type exports for the AIDeveloper frontend.
 * 
 * This file aggregates all type definitions used across the application.
 * 
 * @security All types imported here should be validated at runtime to prevent
 * injection attacks. Never trust client-side type assertions alone.
 */

// Existing types...
// (Assuming existing content; appending new exports)

export * from '../components/ChainBuilder/ChainBuilder.types';

// Add any additional global types here if needed
// export interface GlobalType { ... }